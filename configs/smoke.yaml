mode: standard
results_dir: results/smoke

p: 17
train_fraction: 0.4
seeds: [0]
model_type: quadratic
hidden_width: 64
lr: 0.001
weight_decay: 0.1
init_scale: 0.5
max_steps: 200
batch_size: full

checkpoint_preset: smoke
compute_agop: true
compute_ntk: true
save_final_model: true

agop_batch_size: 64
agop_probe_size: full
agop_exact_p_max: 47
ntk_probe_size: 32
fourier_K: [1, 2, 4, 8, 16]
random_alignment: true
